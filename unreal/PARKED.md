# This track is PARKED (2026-06-10)

The UE5 build was the original target; iteration speed moved the project to
the web build (`src/`, Three.js), which is now the build of record. Everything
under `unreal/` is kept as a future native/visual target and as design
reference. The campaign docs that used to live in `unreal/Docs/` are
superseded by `docs/CAMPAIGN.md` and `docs/MISSION_DESIGN_M1.md`.
